{
  "name": "plantas",
  "version": "1.0.0",
  "private": true,
  "description": "Projeto Casa — plantas 2D/3D (Leaflet + Three.js)",
  "scripts": {
    "build": "node scripts/build-config.js",
    "dev": "node scripts/build-config.js && npx serve ."
  }
}
